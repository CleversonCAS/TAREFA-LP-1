#ifndef FUN_H
#define FUN_H

	void negativo( int *first , int *last );//nega os elementos do vetor										##funfa
	void printv(int *first , int *last);//imprime todo o vetor													##funfa
	const int *min( const int *first , const int *last );//retorna um ponteiro para a posição do menor elementos##NOP
	void reverse(int *first, int *last);

#endif